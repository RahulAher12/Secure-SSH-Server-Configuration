# Secure-SSH-Server-Configuration
Secure SSH Server configuration using Amazon Linux, Ubuntu, RedHat Linux etc.
1. First of all create ssh file using (.sh) extension
2. then all command run maunally bash command line
3. maunally command enter one by one but one ssh file create and all command type and run the command
